import SendMessage from '../views/SendMessage.vue';
import ConvoHistory from '../views/ConvoHistory.vue';

const routes = [
  { path: '/send-message', component: SendMessage },
  { path: '/conversation-history', component: ConvoHistory },
];

export default routes;
